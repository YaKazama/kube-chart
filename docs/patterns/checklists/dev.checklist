# AI 开发校验清单

- [ ] 已读取目标模板、当前正式 SDD 与适用的 `patterns/rules/`。
- [ ] 必填字段、类型、默认行为和互斥关系符合正式 SDD。
- [ ] 多层取值使用 `base.get`，字段渲染使用对应基础能力。
- [ ] map、list 和多类型输入已按 `const-boundary.md` 处理 Helm 4.2.2 兼容性。
- [ ] 合并、列表或上下文修改已通过 `mustDeepCopy` 隔离。
- [ ] 模板命名、分层、字段顺序与输出结构符合架构规则和 Kubernetes API。
- [ ] 最小有效输入、较完整有效输入和关键失败输入已验证。
- [ ] `helm lint` 通过，渲染输出为合法 YAML。
- [ ] 临时验证文件仅在 `/tmp/` 中创建且已清理。
- [ ] 当前变更文档、代码与验证证据一致。
