# 已验证样例与编码片段

本目录保存两类可复用资料：已由 Helm 渲染验证的资源样例，以及与当前实现一致的内部模板编码片段。

- 资源样例按资源或模板分类存放，包含 values、模板调用和 YAML 对照；每个样例注明适用正式 SDD、Helm 版本和验证命令。
- 编码片段仅展示当前实现可复用的局部模式；必须链接真实实现，不替代正式 SDD 或全局规则，也不作为独立渲染证据。
- 示例不能包含未经确认的模板名、常量键或行为；涉及 `fromYaml`、`fromYamlArray` 时必须展示对应的 Helm 4.2.2 兼容性保护。
