# 正式 SDD 规则

- `specs/` 仅保存已实现、已验证、已 Review 的当前正式契约。
- 通用规则统一引用 `patterns/`；正式 SDD 仅定义当前模板专属逻辑。
- 正式 SDD 必须自包含，不得引用 `changes/` 或 `archive/`。
- 通用边界仅引用 `const-boundary.md`；仅补充当前模板独有的边界。
- 正式 SDD 不包含任务步骤、推翻方案、临时假设或过程讨论。
- 新迭代以现有正式 SDD 为基线；验收后更新同一份正式 SDD，Git 负责历史追溯。
