# AI 开发校验清单

## 强制校验项（开发完成必须全部通过）

- [ ] 有没有尽早报错退出?
- [ ] **Fail-Fast 校验**：所有必填字段均已添加 `required` 校验，缺失立即报错?
- [ ] **类型一致性校验**：`values.yaml` 中所有字段类型唯一，无 string/list/dict 混用?
- [ ] **合并逻辑校验**：列表合并使用 `mustCompact` + `mustUniq`；字典合并参数顺序符合优先级预期?
- [ ] **标签逻辑校验**：`justNameLabel`、`helmLabels`、自定义 `labels` 三者互斥与合并逻辑正确?
- [ ] **FileRefs 校验**：所有文件引用均为相对路径，字段格式匹配目标资源类型?
- [ ] **命名规范校验**：模板文件名、命名模板名称符合统一命名规则?
- [ ] **取值规范校验**：无绕过 `base.get` 的手动深层取值?
- [ ] **静态语法校验**：helm lint 零错误通过
- [ ] **渲染正确性校验**：示例配置可正常渲染，输出为合法 YAML
- [ ] **异常场景校验**：非法输入可触发预期报错，无静默失败
- [ ] **规范符合性校验**：所有开发规则与命名规范全部达标

## 静态校验命令

```bash
# 语法与规范校验，零错误通过
helm lint .

# 示例场景渲染测试，无报错且输出合法 YAML
helm template test . -f examples/basic.yaml

# Schema 合法性校验
helm schema validate .
```

## 场景化测试要求

- `ci/` 目录必须覆盖核心场景的测试 values 文件
- 必测场景：默认配置、全量配置、边界值配置、异常配置
- 异常配置必须触发预期的 `fail`/`required` 报错，不得静默生成无效资源
