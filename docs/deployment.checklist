# AI 开发交付检查清单

### 结构与元数据

- [ ] `Chart.yaml` 已声明 `type: library`，核心字段完整合规
- [ ] 目录结构符合规范，`templates/` 下所有文件均以下划线开头
- [ ] `values.yaml`、`values.schema.yaml` 文件齐全，类型定义一致
- [ ] `examples/` 目录包含可直接运行的调用示例
- [ ] `README.md` 包含导入方式、核心模板说明与使用示例
- [ ] 结构完整性：目录与必填文件齐全
- [ ] 文档完整性：README、examples、注释齐全可用、docs 有较详细的文档
- [ ] 测试覆盖性：ci 目录覆盖核心场景与边界场景
- [ ] 安全性：无硬编码敏感信息、权限配置符合最小化原则
- [ ] 兼容性：符合声明的 Kubernetes 与 Helm 版本基线

### 模板开发规范

- [ ] 所有命名模板命名符合「目录前缀.驼峰功能名」规则
- [ ] 每个 `define` 块均有功能、入参、示例注释
- [ ] 统一使用 2 空格缩进，空格控制正确，无多余空行
- [ ] 无臆造函数，所有函数均为 Helm 4.2.2 支持
- [ ] 临时变量使用 `$_` 前缀，作用域清晰

### 核心逻辑合规

- [ ] 所有多层取值均通过 `base.get` 处理，无手动链式取值
- [ ] 类型合并策略正确：字符串替换、列表去空去重、字典递归合并
- [ ] 取值优先级顺序正确，无层级混乱
- [ ] 必填字段均使用 `required` 校验，遵循 Fail-Fast 原则
- [ ] `values.yaml` 字段类型唯一，无类型混用
- [ ] 标签互斥逻辑正确，元数据生成统一走 base 层

### 校验与测试

- [ ] `helm lint` 执行无错误、无严重警告
- [ ] 示例配置可正常渲染，输出为合法 YAML
- [ ] 异常配置可触发预期报错，无静默失败
- [ ] 资源字段严格对齐 Kubernetes v1.36.0 API 规范
