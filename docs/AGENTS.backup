# kube-chart AI 开发指南

面向 AI 的 Helm Library Chart 规范。
适配基线：K8s API >= v1.36.0 | Helm >= 4.0.0 (推荐 4.2.2) | 仅使用 Helm 原生内置函数（原生 Go Template + Sprig 函数 + Helm 特有函数），禁臆造内置函数。

- 角色: 资深 DevOps 工程师 / 云原生架构师
- 目标: 标准化输出、低幻觉、类型稳定、代码 DRY、符合 Helm 最佳实践

## 核心设计原则

- 尽早报错: 必填缺失或类型非法时，使用 required/fail 立即中断；严格校验，快速失败
- 类型唯一: 确保 values.yaml 字段类型的确定性与单一性，严禁同一字段动态混用不同类型
- 最小化传参: 避免无意义透传全局上下文 `.`；单值传标量，同构集合传 list，多维复杂状态才用 dict 聚合
  - 最小化传针对业务资源模板，优先传递业务配置本身；base 层通用工具模板因需要访问全局 Values/Release 等信息，允许传入根上下文
- 状态隔离: 字典合并必须 mustDeepCopy，严禁篡改 `.Values` 全局上下文。以下场景必须执行深拷贝:
  - 从 `.Values`/`.Context` 中取出字典/列表后，执行 merge/set/append 等修改操作前
  - 向子模板传递聚合配置，且配置包含来自全局上下文的引用对象时
  - 伴生资源复用主资源的元数据、标签、镜像等配置时
  - 执行字典合并操作，且合并目标来源于全局上下文时
- 正则集中管理：所有正则校验表达式统一在 `base/_env.tpl` 中维护，按分类嵌套字典组织，最终键名采用全大写下划线分隔。调用范式:

  ```go
  {{- $const := include "base.env" "" | fromYaml }}
  {{- if not (regexMatch $const.DNS.SUBDOMAIN_REGEX $name) }}
    {{- fail "[base.metadata] name: 不符合 DNS 子域名命名规范" }}
  {{- end }}
  ```

## 目录结构

```text
Chart.yaml          # 必须包含 type: library, kubeVersion: ">=1.36.0", 禁 `type: application`
README.md           # 包含父 Chart 导入方式、核心模板说明与使用示例
values.yaml         # 默认数据结构
values.schema.yaml  # JSON Schema YAML 版
values.schema.json  # 校验父 Chart 传入值，默认 {}
docs/               # 文档与校验清单
examples/
  example/          # 可选：application 类型 Chart，用于集成调试
  xxx.yaml          # 使用示例，变量标注类型、含义、默认值
templates/
  base/             # 基础原子能力，单向依赖，禁引用上层模板
  api-resources/    # K8s 原生资源，按 API Group 划分子目录
  cloud/            # 云厂商适配，按厂商划分子目录，禁交叉依赖
  extensions/       # 第三方 CRD，按项目划分子目录，与原生资源解耦
```

分层约束:

- base: 通用取值/校验/字符串/报错原子能力，无上层依赖
- api-resources: 原生 API 命名模板；入参遵循“最小化传参”；字段严格对齐 API；必填项 `required` 校验；元数据复用 base 层、禁止重复实现
- cloud: 云平台适配
- extensions: 明确声明 CRD 版本；入参/校验规则与原生资源一致

## 开发规范

### 命名模板

- 文件命名: `templates/` 下全以 `_` 开头(NOTES.txt 除外)；`base/` 为 `_<小写>.tpl`，其余为 `_<大驼峰>.tpl`
- 模板命名: 目录前缀.小驼峰
  - 目录前缀取最末一级目录名，多级父目录不纳入前缀。例 `api-resources/apps/_Deployment.tpl` 中的模板为 `apps.deployment`
- 注释: 每个 define 块必含：功能说明、入参结构、返回值、示例
- 语法:
  - `{{- define "x.y" -}}...{{- end }}`，2 空格缩进
  - 排版控制: 仅必要时使用 `{{- nindent 0 "" -}}` 强制换行并清除行首缩进（用于打破当前深层缩进上下文，使生成的 YAML 排版更友好）
  - UTF-8 编码；临时变量用 `$_`/`$__` 前缀，局部用 `$var`；禁臆造函数
  - 报错信息格式: `[模板名] 字段路径: 错误原因`

    ```go
    {{- fail "[apps.deployment] image.repository: 镜像仓库地址为必填项" -}}
    ```

### base.get 核心机制

统一取值函数，用于从多层上下文中安全取值、类型转换与合并。返回 YAML 字符串，常配合 `fromYaml` 使用。

- 入参: (list <上下文> <目标键名> [强制类型] [合并模式] [必填校验] [调试日志])
  - 上下文(context): 根上下文，通常为 `.`，必填
  - 目标键名(key): 点分取值路径(支持多级嵌套，如 image.repository)，字符串，必填
  - 强制类型(type): (可选)强制类型转换，支持 int/int64/float64/atoi/toString/toStrings/toDecimal/quote/squote
  - 合并模式(mergeMode): (可选)集合类合并模式，默认 left
    - 字典: left 左优合并(mustMerge)、right 右优覆盖(mustMergeOverwrite)
    - 列表: concat 拼接去重(默认)、replace 全量替换
  - 必填校验(required): (可选)布尔值，是否为必填项，为 `true` 时取值为空立即中断报错，默认 `false`
  - 调试日志(debug): (可选)布尔值，开启后输出调试日志，默认 `false`
- 取值优先级: CLI 显式覆盖参数 > 模板内部上下文 > `.Context` > `.Values` > `.Values.global`
- 合并策略:
  - string: 高优先级覆盖低优先级，取第一个有效值
  - list: 拼接(concat)、去空(compact/mustCompact)、去重(uniq/mustUniq)
    - 判定为空（跳过/剔除）：`nil`、空字符串 `""`、空列表 `[]`、空字典 `{}`
    - 判定为有效值（保留）：`false`、`0`、`"0"`、仅含空白的字符串（需去空白请显式调用 trim）
    - 去重仅对标量元素列表生效；复杂对象列表仅执行拼接、去空，不强制去重
  - dict:
    - 默认左优，使用 merge/mustMerge
    - right 模式右优，使用 mergeOverwrite/mustMergeOverwrite
    - 合并 `.Values` 嵌套字典时必须 mustDeepCopy 防污染 (例: `mustMergeOverwrite (mustDeepCopy $dest) $src`)
- 边界行为:
  - 目标路径不存在且 `required: false`：返回对应类型零值（字符串返回空串、列表返回空列表、字典返回空字典）
  - 目标路径不存在且 `required: true`：输出标准化报错并立即中断渲染
  - 返回值始终为合法 YAML 标量/结构，保证 fromYaml 解析无异常
- 调用范式:

  ```go
  // 基础取值
  {{- $targetVal := include "base.get" (list . "key") | fromYaml }}
  // 强制类型 + 必填校验
  {{- $repo := include "base.get" (list . "key" "toString" "" true) | fromYaml }}
  // 字典右优覆盖合并
  {{- $labels := include "base.get" (list . "key" "" "right") | fromYaml }}
  ```

### base.field 核心机制

用于将键值对安全渲染为 YAML 格式，自动处理引号、枚举校验及特殊类型

- 入参: (list <key> <value> [渲染模板/quote] [允许值列表])
  - key: 字符串，YAML 键名（必传）
  - value: 任意类型，YAML 值（必传）
  - 渲染模板: (可选) 指定处理 value 的模板名
    - base.string（默认）：字符串类型自动加双引号，数字/布尔/Null 原生输出，自动转义特殊字符
    - base.int/base.int64/base.float64: 数值类型原生输出，非数值类型触发报错
    - quote: 强制转为字符串并加双引号（常用于处理 `[a,b,c]` 列表转字符串）
    - containers.env: 专用模板，处理 K8s env 列表中的 value 字段，字符串不加引号，兼容变量引用语法
  - 允许值列表: (可选) 枚举校验，若传入则强制使用 base.string 渲染；值不在允许列表中时，立即输出标准化报错并中断渲染
- 调用范式:

  ```go
  // 常规渲染
  {{- include "base.field" (list "replicas" $replicas "base.int") }}
  // 强制加引号
  {{- include "base.field" (list "tag" $tag "quote") }}
  // 枚举校验 (只允许 "Always" / "IfNotPresent")
  {{- include "base.field" (list "pullPolicy" $policy "base.string" (list "Always" "IfNotPresent")) }}
  ```

### 核心能力

- 元数据和标签:
  - 名称: fullname > name。名称长度由 base 层名称能力统一校验，超出 63 字符时触发报错中断，禁止静默截断，避免资源名不可预期，符合「尽早报错」原则
  - 命名空间: namespace，默认 default
  - 标签/注解: helmLabels/justNameLabel/labels 互斥融合；annotations 同逻辑
    - helmLabels: 生成标准 Helm 标签集
    - justNameLabel: 为 `true` 时，丢弃所有默认标签，仅保留 app.kubernetes.io/name
    - labels: 与默认标签集合并，用户标签同键覆盖默认标签
- 镜像: 内联 image > imageRef。
  - image: 字符串，完整镜像地址
  - imageRef: 结构化对象，`{registry, namespace, repository(必填，缺失报错), tag, digest}`，拼接规则为 `registry/namespace/repository:tag@digest`，namespace 为空则省略层级
- 外部引用（*FileRefs）:
  - 优先级: `*FileRefs` > 结构化字段 > 原生内联字段
  - 通用约束:
    - filePath 必须为 Chart 内相对路径，禁绝对路径，使用 base.uriPath 校验和清洗路径
  - 分类场景:
    - 容器级: envFileRefs/envFromFileRefs/resourcesFileRefs/imageFileRefs，支持 fieldPaths 点分取值
    - 配置级: dataFileRefs/binaryDataFileRefs/stringDataFileRefs，支持指定 key(key 为空则取文件名)
  - 调用范式:

    ```yaml
    envFileRefs:
      - filePath: "config/app-env.yaml"   # 必填，相对路径
        fieldPaths: "path.to.key"         # 文件内取值路径 (点分格式)，可为空

    dataFileRefs:
      - filePath: "config/nginx.conf"     # 必填，相对路径
        key: "nginx.conf"                 # 映射到 K8s 的 key。若为空，则自动使用文件名 (nginx.conf)
    ```

- 伴生资源:
  - 工作负载等主资源可通过嵌套配置字段，自动生成一对一绑定的配套资源。
  - 适用范围：与主资源强绑定、一对一存在的配套资源，包括 Service、ServiceAccount、PodDisruptionBudget 等；Ingress 仅在单后端 Service 的场景下可作为伴生资源实现，多后端场景由父 Chart 独立编排
  - 命名规则:
    - 必须调用底层 `base.name` 获取名称，其内部已实现 `fullname > name` 的优先级降级及 RFC1035 强校验
    - 若发生类型冲突（如 StatefulSet 与 Headless Service），必须使用约定式固定后缀（如 `-headless`），禁用随机函数或动态哈希，以保证幂等性
  - 渲染与分隔符规则：
    - 配置为空时不渲染、不输出任何内容
    - 仅当伴生资源真实渲染时，在其前置输出 `---` 分隔符
    - 库模板首尾严禁输出 `---`
  - 数据隔离：从主资源向伴生资源传递元数据、标签等配置时，必须使用 mustDeepCopy 防污染
  - 结构一致性：伴生资源的配置字段，与对应独立资源模板的入参结构完全一致

  ```yaml
  {{- /* 构造独立上下文：满足条件时将 service 配置合并进 Context，不污染根上下文 */ -}}
  {{- $ctx := mustDeepCopy . -}}
  {{- if and .Context .Context.service (not .Context.services) -}}
    {{- $mergedContext := mustMergeOverwrite (mustDeepCopy .Context.service) .Context -}}
    {{- $ctx = mustMergeOverwrite $ctx (dict "Context" $mergedContext) -}}
  {{- end -}}
  ```

## 职责边界

- 库 Chart:
  - 渲染主资源+伴生资源
  - 分隔符规范: 库模板首尾严禁输出 `---`；仅在主、伴生资源之间输出 `---` 分隔
  - 合并伴生资源时必须 mustDeepCopy
  - 禁 range 循环渲染多个独立资源并拼接 `---`
  - 禁处理 Helm Hooks
- 父 Chart:
  - 编排独立跨资源（`templates/` 下用 `---` 分隔各资源调用），管理 Helm Hooks
  - **标准调用范式 (上下文注入)**:

    ```yaml
    ---
    {{- $ctx := merge (dict "Context" .Values.Deployment) . }}
    {{- include "workloads.Deployment" $ctx }}
    ```

## 行为准则

### 内置安全基线

在元数据 / 工作负载模板中默认遵循 Pod 安全标准，提供可覆盖的默认安全上下文:

- 默认启用 runAsNonRoot: true、readOnlyRootFilesystem: true
- 禁止默认开启 privileged、hostNetwork 等高风险权限

### 禁令

- 绕开 base.get 手写多层取值逻辑
- values.yaml 同一字段使用混合类型
- 用默认值兜底必填项缺失
- base 层依赖上层模板
- `templates/` 下出现非下划线开头的模板文件（NOTES.txt 除外）
- 使用 Helm 原生不支持的自定义内置函数
- 硬编码密钥、证书、敏感凭据等信息
- 任何密钥、令牌、证书私钥出现在 values.yaml 默认值中
- 库 Chart 使用 `range` 循环渲染多个独立资源并自行拼接 `---` 分隔符（*允许: 资源内部列表循环，如 containers、env、ports*）
- 库 Chart 模板内处理 Helm Hooks
- 在父 Chart 中直接篡改 `.Values` 原始结构
- 在父 Chart 中直接使用 `set .` 修改全局上下文（必须使用 merge 构造局部 `$ctx` 传入）

### 开发自检

- 必填字段是否全部添加 `required` 校验
- 列表合并是否执行去空、去重
- 字典合并顺序是否符合优先级预期
- 命名是否符合「目录前缀.驼峰功能名」规则
- 是否包含功能、入参、示例注释
- 是否通过 base.get 取值
- 标签互斥逻辑是否正确
- `*FileRefs` 的路径是否为相对路径
- `*FileRefs` 路径是否经过 base 层校验
- 伴生资源合并是否使用 `mustDeepCopy` 防污染
- 库模板首尾是否误加了 `---`
- 父 Chart 连续调用多个资源时，是否每次都通过 `merge` 构造了独立的局部 `$ctx`，避免了上下文串用

### 校验入口

- 触发「校验检查」/「checklist」: 读 `./docs/dev.checklist`。
- 触发「交付检查」/「deploy checklist」: 读 `./docs/deployment.checklist`。

### 交互协议

```text
目标: [动作(新增(add)/修改(edit)/重构(refactor)/审查(review)/schema)+文件/模板路径]
动作: [新增/修改/重构/审查/schema]
需求:
  - [需求 1]
约束: [可选]
  - [约束 1]
参考/上下文: [可选，文件引用或 URL]
```

- 新增(add)：指定相对路径，未指定则提示输入
  - 触发「创建 base 模板」+模板名称（如 base.uriPath）或「创建 base 命令模板」+模板名称（如 base.relPath）: 新增 base 层命名模板

### 文档标准

#### 统一 README 结构

强制 README 包含固定模块，降低使用者理解成本。

- 安装与导入方式
- 核心设计原则摘要
- 可用模板清单（模板名 + 功能简介 + 入参链接）
- 最小可用示例
- 常见问题与迁移说明

#### 规范 values.yaml 注释

每个字段必须包含「类型 + 含义 + 是否必填 + 默认值」注释，嵌套字段逐层标注，与 Schema 一一对应

### Schema 单源原则

- Helm 原生仅加载 `values.schema.json` 执行校验；`values.schema.yaml` 为维护源码，发布前通过工具转换为 JSON 文件，禁止直接修改 JSON
- `values.schema.json` 初始状态为空对象 `{}` 代表无校验；同步迭代 Schema 定义，确保 values 结构与 Schema
- 强制 Schema 校验粒度：所有顶层字段必须声明类型，必填字段必须列入 required，枚举值必须显式声明
