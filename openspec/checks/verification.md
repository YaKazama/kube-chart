# 一致性验证检查

- [ ] 当前阶段为 `implemented` 或 `verified`，批准有效且冻结摘要匹配。
- [ ] `records/verification.md` 的“实施检查（必须）”为通过，冻结摘要和实现文件摘要与当前内容一致。
- [ ] 以冻结 spec 和 draft 验收为预期，以当前正式实现为实际。
- [ ] Helm lint、最小有效、较完整有效、关键失败输入和受影响回归均有独立实际结果。
- [ ] 未实现子模板假设满足冻结的最小正确返回契约；只验证当前目标，不声称真实子模板集成。
- [ ] 临时同名 `define` 只存在于 `/tmp/`，验证记录明确其用途且完成后已清理。
- [ ] “独立验证（可选）”包含环境、输入或命令、预期、实际和通过/失败结论，且未覆盖实施检查。
- [ ] 验证阶段未修改正式代码、draft、plan 或当前规格。
- [ ] 验证状态只写入 draft frontmatter 和 `records/verification.md`。
