# Apply 执行提示

本文件只提示 `/sdd-apply` 的范围和开发反馈，不是验证证据。

- [ ] 当前阶段为 `approved` 或 `applied`，批准有效，`draft-content-sha256` 和冻结文件摘要全部匹配。
- [ ] 只修改冻结目标授权的正式代码；依赖占位和非目标未被当作授权。
- [ ] 实现符合全部 Requirement 与 Scenario，没有按现有代码反写规格。
- [ ] 必填、类型、默认行为、零值、互斥关系和错误路径符合冻结契约。
- [ ] 命名、分层、字段顺序、上下文隔离和输出结构符合适用工程规则。
- [ ] 未实现子模板只保留真实调用边界，不在正式 `templates/` 创建空或假数据 `define`。
- [ ] 开发反馈只来自仓库真实存在并实际执行的项目命令；没有臆造 `make`、Helm、npm 或自定义校验器。
- [ ] 模板变更至少实际运行 `/opt/homebrew/bin/helm lint`，并按失败结果继续修复或如实报告。
- [ ] 未创建或更新 `records/verification.md`；正式验证留给 `/sdd-verify` 基于当前实现重新执行。
- [ ] 未合并当前规格；`/tmp/` 临时资产已清理。
- [ ] 已冻结 plan 未在实施阶段被改写；完成后进入或保持 `applied`，未完成或真实反馈命令失败时保持进入命令前的稳定状态。
