# 实施检查

- [ ] 当前批准有效，`draft-content-sha256` 和冻结文件摘要全部匹配。
- [ ] 只修改冻结目标授权的正式代码；依赖占位和非目标未被当作授权。
- [ ] 实现符合全部 Requirement 与 Scenario，没有按现有代码反写规格。
- [ ] 必填、类型、默认行为、零值、互斥关系和错误路径符合冻结契约。
- [ ] 命名、分层、字段顺序、上下文隔离和输出结构符合适用工程规则。
- [ ] 未实现子模板只保留真实调用边界，不在正式 `templates/` 创建空或假数据 `define`。
- [ ] 受影响能力回归通过。
- [ ] 模板变更的 `/opt/homebrew/bin/helm lint`、最小有效、较完整有效和关键失败输入通过。
- [ ] `records/verification.md` 的契约摘要、实现文件摘要和“实施检查（必须）”已按实际结果创建或更新。
- [ ] 实现或直接依赖变化后，旧的独立验证已标记为失效或未执行。
- [ ] 未合并当前规格；`/tmp/` 临时资产已清理。
- [ ] 已冻结 plan 未在实施阶段被改写；进度只记录于 draft frontmatter `status`。
