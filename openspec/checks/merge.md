# 合并检查

- [ ] 当前阶段为 `implemented` 或 `verified`，批准有效，正式实现已完成。
- [ ] 用户执行 `/sdd-merge`，明确确认人工 Review 已完成；未用 AI 自行触发代替。
- [ ] 冻结摘要匹配，当前实现与冻结 spec 无已知偏差。
- [ ] `records/verification.md` 存在，“实施检查（必须）”通过，冻结摘要和实现文件摘要与当前内容一致。
- [ ] “独立验证（可选）”为未执行时明确报告 `skipped`；已执行时必须通过，失败或失效均阻止合并。
- [ ] delta 已正确合并到当前规格，只保留已实现的可观察契约。
- [ ] README、`docs/` 和稳定样例只描述当前可用行为；未把临时替身描述为真实集成。
- [ ] 文档链接和适用发布检查通过。
- [ ] 归档目录日期与 change-id 正确，draft frontmatter 最终状态为 `merged`。
